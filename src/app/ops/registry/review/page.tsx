import { RegistryReviewView } from "@/features/resident-household-registry/views/registry-review-view";

export const metadata = { title: "Review queues · Staff workspace" };

export default function Page() {
  return <RegistryReviewView />;
}
