import { ResidentDirectoryView } from "@/features/resident-household-registry/views/resident-directory-view";

export const metadata = { title: "Residents · Staff workspace" };

export default function Page() {
  return <ResidentDirectoryView />;
}
