import { LedgerFormView } from "@/features/sectoral-assistance/views/ledger-form-view";

export const metadata = { title: "New ledger entry" };

export default function Page() {
  return <LedgerFormView />;
}
