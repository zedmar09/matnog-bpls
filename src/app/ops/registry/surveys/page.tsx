import { RegistrySurveysView } from "@/features/resident-household-registry/views/registry-surveys-view";

export const metadata = { title: "Survey assignments · Staff workspace" };

export default function Page() {
  return <RegistrySurveysView />;
}
