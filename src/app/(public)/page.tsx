import styles from "@/features/bpls-shell/bpls-shell.module.css";

export default function Page() {
  return <main aria-label="BPLS workspace" className={styles.emptyPage} id="main-content" tabIndex={-1} />;
}
