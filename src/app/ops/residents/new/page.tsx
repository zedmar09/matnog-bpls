import { ResidentRegistrationView } from "@/features/resident-household-registry/views/resident-registration-view";

export const metadata = { title: "Register a resident · Staff workspace" };

export default function Page() {
  return <ResidentRegistrationView />;
}
